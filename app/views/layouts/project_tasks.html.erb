<!DOCTYPE html>
<html class="<%= dark_mode_class %>">
  <head>
    <%= render 'layouts/head' %>
  </head>

  <body class="dark:bg-gray-950">
    <%= render 'layouts/project_nav' %>

    <div class="my-4 pb-4 border-b dark:border-gray-600">
      <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl px-4">
        <div class="flex flex-grow items-center">
          <%= project_tabs "Tasks" %>
        </div>

        <%= render 'layouts/project_buttons' %>
      </div>
    </div>

    <main class="container mx-auto max-w-screen-xl p-4 flex -mt-4">
      <div class="w-full">
        <div class="mb-4 md:mb-0 lg:w-3/4 mx-auto">
          <div class="flex flex-col md:flex-row md:space-x-12">
            <div class="mb-4 md:mb-0 md:w-full">
              <%= render 'project/tasks/nav' %>
              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
