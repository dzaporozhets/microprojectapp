<!DOCTYPE html>
<html>
  <head>
    <title>MicroProject.app</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-gray-100">
      <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl px-4">
        <div class="flex flex-grow items-center">
          <ol role="list" class="flex space-x-4 flex-grow min-w-0">
            <li class="flex">
              <%= render 'layouts/home_link' %>
            </li>
            <li class="flex flex-grow min-w-0">
              <%= render 'layouts/separator' %>

              <div class="flex items-center ml-4">
                <%= link_to @project do %>
                  <div class="font-semibold leading-6 text-gray-900 text-base line-clamp-1">
                    <span><%= @project.name %></span>

                    <% if @project.archived? %>
                      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-5 w-5 text-gray-400 inline ml-2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="m20.25 7.5-.625 10.632a2.25 2.25 0 0 1-2.247 2.118H6.622a2.25 2.25 0 0 1-2.247-2.118L3.75 7.5M10 11.25h4M3.375 7.5h17.25c.621 0 1.125-.504 1.125-1.125v-1.5c0-.621-.504-1.125-1.125-1.125H3.375c-.621 0-1.125.504-1.125 1.125v1.5c0 .621.504 1.125 1.125 1.125Z" />
                      </svg>
                    <% end %>
                  </div>
                <% end %>
              </div>
            </li>
          </ol>
          <div class="flex items-center space-x-6 rtl:space-x-reverse hidden md:flex ml-4">
            <%= render 'layouts/user' %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto max-w-screen-xl p-4 flex">
      <div class="w-full">
        <div class="mx-auto">
          <div class="flex flex-col md:flex-row md:space-x-12">
            <div class="mb-4 md:mb-0 md:w-2/3">
              <%= yield %>
            </div>
            <div class="mb-4 md:mb-0 md:w-1/3">
              <%= render 'projects/team' unless @project.personal? %>
              <%= render 'projects/files' %>
              <%= render 'projects/links' %>
              <%#= render 'projects/activity' unless @project.personal? %>
              <%= render 'projects/extra' if !@project.personal? && @project.user == current_user %>
            </div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>

