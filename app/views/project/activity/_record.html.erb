<li class="ml-4 sm:ml-6">
  <div class="absolute w-3 h-3 avatar-bg mt-1.5 -left-1.5 border border-white dark:border-gray-900"></div>
  <div class="rounded-lg">
    <div class="flex justify-between items-start">
      <div>
        <p class="mt-0.5 text-sm text-muted">
          <span class="font-medium text-gray-900 dark:text-gray-200 text-sm"><%= record.user.email %></span>
          <%= record_to_text(record) %>
        </p>
      </div>
      <time datetime="<%= record.created_at.iso8601 %>" class="flex-none py-0.5 text-xs leading-5 text-muted">
        <%= time_ago_short record.created_at %>
      </time>
    </div>
  </div>
</li>
