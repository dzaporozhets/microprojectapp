<div class="flex justify-between items-center mt-5 mb-3 pb-3 border-b dark:border-gray-600">
  <h1 class="font-bold text-xl dark:text-gray-200 leading-6">Activity</h1>
</div>

<div class="mt-8">
  <ul role="list" class="space-y-6 mt-2">
    <% @records.each do |record| %>
      <%= render 'project/activity/record', record: record %>
    <% end %>
  </ul>
</div>
