<% if @task.star %>
  <%= turbo_stream.remove("task_#{@task.id}") %>
  <%= turbo_stream.prepend('tasks', partial: "project/tasks/task", locals: { task: @task }) %>
<% else %>
  <%= turbo_stream.replace "#{dom_id(@task)}-star", partial: 'project/tasks/star', locals: { task: @task } %>
<% end %>
