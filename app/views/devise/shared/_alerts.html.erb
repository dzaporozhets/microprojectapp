<% if defined?(notice) && notice.present? %>
  <p class="my-5 p-4 mb-4 text-sm text-blue-800 rounded-lg bg-blue-50"><%= notice %></p>
<% end %>

<% if defined?(alert) && alert.present? %>
  <p class="my-5 p-4 text-sm text-gray-800 rounded-lg bg-gray-50"><%= alert %></p>
<% end %>
