<div>
  <% content_for :title, "Projects" %>

  <% content_for :subnav do %>
    <div class="nav-container">
      <div class="nav-title flex items-center justify-between">
        <%= link_to projects_path do %>
          <div class="nav-title-link">
            <span>Projects</span>
          </div>
        <% end %>

        <%= link_to "New Project", new_project_path, class: "btn-base" %>
      </div>
    </div>
  <% end %>


  <div>
    <div class="space-y-4">
      <% if @pinned_projects.present? %>
        <div class="resource-container">
          <div>
            <ul id="pinned-projects" role="list" class="resource-list">
              <% @pinned_projects.each do |project| %>
                <%= render project, pinned: true %>
              <% end %>
            </ul>
          </div>
        </div>
      <% end %>

      <% if @projects.present? %>
        <div class="resource-container">
          <div>
            <ul id="projects" role="list" class="resource-list">
              <% @projects.each do |project| %>
                <%= render project %>
              <% end %>
            </ul>
          </div>
        </div>
      <% end %>

      <% if @archived_projects.any? %>
        <div class="resource-container">
          <div>
            <ul id="archived-projects" role="list" class="resource-list">
              <% @archived_projects.each do |project| %>
                <%= render project %>
              <% end %>
            </ul>

            <% if @archived_projects.total_pages > 1 %>
              <div class="mt-4">
                <%= paginate @archived_projects %>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
