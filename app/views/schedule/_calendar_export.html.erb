<div class="resource-container">
  <div class="resource-header">
    <div>
      <h4 class="resource-title">Sync Tasks to Your Calendar</h4>
    </div>
  </div>

  <div class="p-4 space-y-4">
    <!-- URL Subscription Section -->
    <div>
      <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">
        Copy the URL below and paste it into your calendar app to subscribe. New tasks will show up there automatically.
      </p>
      
      <div class="flex items-center gap-2">
        <input
          type="text"
          value="<%= calendar_url(current_user) %>"
          readonly
          class="flex-1 px-3 py-2 text-sm border border-gray-300 dark:border-gray-700 rounded-md bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100"
          onclick="this.select();"
        >
        <button
          class="px-3 py-2 btn-base flex items-center"
          onclick="navigator.clipboard.writeText('<%= calendar_url(current_user) %>'); this.querySelector('span').innerText = 'Copied!'; setTimeout(() => this.querySelector('span').innerText = 'Copy', 2000);"
        >
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4 mr-1">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 01-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 011.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 00-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5m7.5 10.375H9.375a1.125 1.125 0 01-1.125-1.125v-9.25m12 6.625v-1.875a3.375 3.375 0 00-3.375-3.375h-1.5a1.125 1.125 0 01-1.125-1.125v-1.5a3.375 3.375 0 00-3.375-3.375H9.75" />
          </svg>
          <span>Copy</span>
        </button>
      </div>

      <p class="mt-2 text-xs text-gray-500 dark:text-gray-400">
        The URL above contains a secure token. If you suspect it has been compromised, you can regenerate it in your account settings.
      </p>
    </div>

    <!-- Divider -->
    <div class="border-t border-gray-200 dark:border-gray-700"></div>

    <!-- ICS Download Section -->
    <div>
      <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">
        Alternatively, download an .ics file and open it in your calendar app to import your tasks.
      </p>

      <%= link_to calendar_schedule_path(token: current_user.calendar_token, format: :ics), class: "btn-base-sm" do %>
        Download .ics File
      <% end %>
    </div>
  </div>
</div>
